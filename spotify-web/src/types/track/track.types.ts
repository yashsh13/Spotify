export type TrackType = {
    id: string,
    name: string,
    artistName: string,
    coverPhoto: string,
    duration: number,
    genre: ["hip-hop","classical"],
    createdAt: string,
    coverImageUrl: string
}